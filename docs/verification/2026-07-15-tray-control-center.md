# 通知領域コントロールセンターの検証記録

- 日付：2026年7月15日
- 更新：2026年7月17日
- 対象：通知領域フライアウトへの再実装
- OS：Windows 11

## 自動検証

`pwsh -NoProfile -File ./scripts/verify.ps1`を実行しました。
復元、書式検証、Releaseビルド、単体テストはすべて成功しました。

- Coreテスト：20件成功
- Appテスト：40件成功
- 警告：0件
- エラー：0件

配置計算は、下、上、左、右の各タスクバー、負の画面座標、作業領域内への制限をテストしています。
配置計算は通知アイコンの中心を優先し、画面端では12 DIP相当の余白を優先することもテストしています。
モーション計算は各タスクバー方向、ウィンドウ全体が隠れる距離、開くcubic ease-outと閉じるcubic ease-inの中間値および終端をテストしています。
ViewModelは、現在の電源側の初期展開と、現在値に対する未適用状態をテストしています。
電源要求の実状態に応じたAutomation名、カードの折りたたみと再展開、プリセット外の保存値を未適用変更としないこともテストしています。
WPF実体テストは、372 DIPのフライアウト属性、スリープ防止のToggleButton、ACおよびバッテリーのAutomation名を検証しています。
主要な適用操作とAC限定チェックボックスが44 DIP以上の高さを持つことも検証しています。
テーマテストは、ライト、ダーク、高コントラストで意味別のブラシが切り替わることを検証しています。
ViewModelの失敗テストは、AC設定のエラーがバッテリー側へ波及せず、未適用状態が維持されることを検証しています。
離散スライダーのテストは、未適用時だけ現在値リングを表示し、変更後のつまみと区別する判定を検証しています。

## ランタイム確認

電源設定を変更しない状態でRelease成果物を起動しました。
通知アイコンのコールバックと同じWin32メッセージを送り、表示状態が`表示 → 非表示 → 表示`と切り替わることを確認しました。

キーボード選択と同じ通知メッセージではフライアウトが表示され、Escape相当のキー入力後に非表示となりました。
外側へフォーカスが移った場合もフライアウトが閉じることを確認しました。

成功通知の表示中は高さ675物理px、4秒の期限後は高さ602物理pxとなりました。
この結果から、一時通知が期限後に消え、内容に合わせてフライアウトが再配置されることを確認しました。

確認環境は144 DPI（150%）でした。
ウィンドウは372 DIPに相当する558物理pxで描画され、右側の状態表示、補助メニュー、六つのプリセットを含む全幅を確認しました。
ダークテーマでは、本文、チェックボックス、プリセット名が背景から識別できることを描画結果で確認しました。

BatteryMonitorを参照した再設計後の画面を、電源設定へ書き込まない状態で描画しました。
製品名は補助表示、スリープ防止は主状態、ACとバッテリーは独立カードとして表示されています。
バッテリーの選択だけを30分から60分へ動かし、現在値を輪郭リング、変更後をアクセント色のつまみ、フォーカスを外側リングとして同時に確認しました。
「適用」は実行していません。

2026年7月16日の描画確認では、専用チェックボックス、図形によるシェブロン、現在値の簡潔な表記、カード内へまとめた適用操作を確認しました。
描画結果は`displaylight-polished.png`としてCodexの可視化領域へ保存しました。

同日の追加確認では、表示前に通知アイコン側へ配置し、タスクバー側から最終位置へ移る座標変化を確認しました。
初回の16 DIPと160ミリ秒ではフェードの背後で移動が終わって見えたため、32 DIPと220ミリ秒へ変更し、開始時の不透明度を72%としました。
144 DPIの座標ログでは、開始位置から最終位置まで48物理px移動することを確認しました。
144 DPIではウィンドウが558物理pxとなり、372 DIPの指定と一致しました。
最外周の前景サーフェスはWindows 11の標準値である8 DIPへ変更しました。

2026年7月17日、Windows 11のコントロールセンターを録画した参照動画との比較結果を受け、モーションを再検証する実装へ変更しました。
表示時はウィンドウ寸法と配置ギャップを合計した距離だけタスクバー側へ隠し、約250ミリ秒のcubic ease-outで移動します。
終了時は約170ミリ秒のcubic ease-inで同じ位置へ戻り、完了後に非表示とします。
参照動画ではタスクバーによる遮蔽が支配的だったため、開閉中の不透明度は100%を維持します。
初回の実機確認では、完全非表示から始まることとタスクバーが手前へ見えることを確認しました。
一方、固定間隔の`DispatcherTimer`ではフレームレートが低く見え、初動と減速尾の体感は未確定でした。
位置更新を`CompositionTarget.Rendering`へ変更し、最終位置へ到達した後に内容だけを約120ミリ秒でフェードする処理を追加しました。
展開、折りたたみ、状態メッセージによる高さ変更は、位置とサイズを約180ミリ秒で同時に補間する処理へ変更しました。
下タスクバーでは下端を固定して上へ伸び、上および左右タスクバーでも通知アイコン側を基準に伸縮する配置計算を使用します。
Releaseビルドと自動テストは成功しましたが、描画フレーム同期へ変更した後の実機モーションは未確認です。

Windows 11の一時ウィンドウ向けDesktop Acrylic、DWM角丸、ダークモード属性を適用しました。
高コントラストでは背景効果を無効にし、SystemColorsによる不透明サーフェスへ戻します。
PrintWindowによる描画結果では内容を確認できましたが、背景の透過量は利用者のデスクトップ上での目視確認が必要です。

## 未確認

次の項目は実機での利用者確認が必要です。

- マウスによる通知アイコンの左クリックと右クリック
- タスクバーを上、左、右へ移動した場合の配置
- 100%と200% DPI、および異なるDPIの複数モニター
- ライトテーマと高コントラスト
- Desktop Acrylicの透過量と、無効時の単色フォールバック
- 開閉モーションの速度と、DWMおよび前景サーフェスの角丸が目視上で一致すること
- Windowsのクライアント領域アニメーションを無効にした場合の即時開閉
- NarratorによるAC、バッテリー、各プリセット名の読上げ
- Explorer再起動後の通知アイコン再登録
- 補助メニュー操作中にフライアウトが不用意に閉じないこと
